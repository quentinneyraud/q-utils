export const addClass = require('./functions/addClass').default
export const addEvent = require('./functions/addEvent').default
export const clamp = require('./functions/clamp').default
export const debounce = require('./functions/debounce').default
export const gebc = require('./functions/gebc').default
export const gebi = require('./functions/gebi').default
export const getPos = require('./functions/getPos').default
export const hasProperties = require('./functions/hasProperties').default
export const isOneOf = require('./functions/isOneOf').default
export const lerp = require('./functions/lerp').default
export const logElement = require('./functions/logElement').default
export const map = require('./functions/map').default
export const qs = require('./functions/qs').default
export const qsa = require('./functions/qsa').default
export const removeClass = require('./functions/removeClass').default
export const removeEvent = require('./functions/removeEvent').default
export const serializeForm = require('./functions/serializeForm').default
export const setStyle = require('./functions/setStyle').default

export default {
  addClass,
  addEvent,
  clamp,
  debounce,
  gebc,
  gebi,
  getPos,
  hasProperties,
  isOneOf,
  lerp,
  logElement,
  map,
  qs,
  qsa,
  removeClass,
  removeEvent,
  serializeForm,
  setStyle
}
